plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.0.0'
}

group 'com.newrelic'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.apache.commons:commons-lang3:3.12.0'
    implementation 'commons-io:commons-io:2.11.0'
    implementation 'commons-collections:commons-collections:3.2.2'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
}

mainClassName = 'com.newrelic.code.exercise.TopNCommonlyUsedThreeWordPhrases'

shadowJar {
    manifest {
        attributes 'Main-Class': 'com.newrelic.code.exercise.TopNCommonlyUsedThreeWordPhrases'
    }
}


/*
jar {
    manifest {
        attributes(
                'Main-Class': 'com.newrelic.code.exercise.TopNCommonlyUsedThreeWordPhrases'
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
*/

test {
    useJUnitPlatform()
}

run {
    standardInput = System.in
}


